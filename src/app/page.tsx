// Home placeholder — se reemplaza por el sitio real de Torre Correos (piloto Hero
// en curso, construido con el reference lock de Refero + DESIGN.md).
export default function HomePage() {
  return (
    <main className="flex min-h-[100dvh] flex-col items-center justify-center gap-2 px-4 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Torre Correos</h1>
      <p className="max-w-md text-sm text-muted-foreground">Rebuild en curso.</p>
    </main>
  );
}
